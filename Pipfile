[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[dev-packages]
ruff = "==0.3.1"
pre-commit = "==3.6.2"
cfn-lint = "==0.86.0"

[requires]
python_version = "3.12"

[scripts]
ruff-check = "ruff check ."
ruff-format = "ruff format ."
